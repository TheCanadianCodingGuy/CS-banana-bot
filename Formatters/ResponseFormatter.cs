using Discord.WebSocket;
using Microsoft.Extensions.Configuration;

namespace CS_banana_bot.Formatters
{
    public static class ResponseFormatter
    {
        public static async Task<string> FormatHandleTest(string text)
        {
            //assume data being formatted and all that.
            return text;
        }

        public static async Task<string> FormatHandleTimedTest(string text)
        {
            //assume data being formatted and all that.
            return text;
        }
    }
}
